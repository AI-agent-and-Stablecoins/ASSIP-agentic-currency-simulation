"""Investor agent: longer holding horizon than buyer/seller agents.

Phase 1 behavior is identical to BaseAgent -- portfolio rebalancing and
active reallocation strategies are a Phase 3+ extension.
"""

from src.agents.base_agent import BaseAgent


class InvestorAgent(BaseAgent):
    pass
